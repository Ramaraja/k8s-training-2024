apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvtest-claim
  labels:
    app: webserver
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: 1Gi